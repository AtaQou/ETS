# Αναφορά Διορθώσεων Bounding Boxes και Hover Translation Debug

## Πρόβλημα που αντιμετωπίστηκε
- Τα κουτιά OCR (bounding boxes) εμφανίζονταν μετατοπισμένα στο PDF, οπότε η ανίχνευση λέξεων για μετάφραση δεν συνέπιπτε με το οπτικό περιεχόμενο.
- Υπήρχε κενό ανάμεσα στις διαστάσεις εικόνας που χρησιμοποιεί το back-end για OCR και στο πραγματικό μέγεθος του καμβά που αποδίδει το front-end.

## Βασικές διορθώσεις στα κουτιά
- **Μεταφορά διαστάσεων σελίδας από το back-end**: Το endpoint `words-positions` προσθέτει τώρα `width` και `height` για κάθε σελίδα, δηλώνοντας τις διαστάσεις της εικόνας που πέρασε από OCR.
- **Ενιαία κλιμάκωση συντεταγμένων**: Η `calculateScaledPositions` χαρτογραφεί τα raw κουτιά στις διαστάσεις του πραγματικού καμβά PDF (DOM `canvas` με `data-page-number`) χρησιμοποιώντας τις διαστάσεις που στέλνει ο server. Έτσι οι συντεταγμένες X/Y ευθυγραμμίζονται με την προβολή στην οθόνη.
- **Κοινά δεδομένα για overlay και λογική μετάφρασης**: Τόσο το `WordBoxesOverlay` (debug) όσο και το `TextBox` αντλούν τα ίδια `wordPositions` από το context και εφαρμόζουν τον ίδιο helper κλιμάκωσης. Τα κόκκινα κουτιά που βλέπει ο χρήστης είναι ακριβώς τα κουτιά που χρησιμοποιεί η εφαρμογή για ανίχνευση λέξεων.
- **Σταθερή αναφορά σελίδων**: Κάθε rendered σελίδα αποκτά προβλέψιμα wrappers/γνωρίσματα (`data-page-number`) ώστε τα overlays να εντοπίζουν τον σωστό καμβά ακόμη και με διαφορετικά ύψη/κενά μεταξύ σελίδων.

## Τι ήταν λάθος πριν
- Το front-end υπέθετε σταθερά περιθώρια/ύψη και δεν γνώριζε τις πραγματικές διαστάσεις εικόνας OCR, οδηγώντας σε διαφορετικές κλίμακες μεταξύ back-end και PDF viewer.
- Χρησιμοποιούνταν χειροκίνητες διορθώσεις/offsets και μία μόνο αναφορά καμβά, με αποτέλεσμα συστηματική μετατόπιση των κουτιών όταν οι σελίδες είχαν διαφορετικά μεγέθη ή όταν άλλαζε το zoom.

## Νέα λειτουργία: Hover Translate (Debug)
- Προστέθηκε νέα ρύθμιση στις Settings, ακριβώς κάτω από το "Show word boxes": **"Hover translate (debug)"**. Όταν είναι ενεργή, η μετάφραση ενεργοποιείται με τον κέρσορα πάνω σε ένα κουτί.
- Η ρύθμιση αποθηκεύεται στα user settings (τύποι και αρχικές τιμές) ώστε να παραμένει συνεπής ανά χρήστη.

## Ροή μετάφρασης: Eye-Tracker και Hover (ταυτόσημη διαδρομή)
1. **Φόρτωση δεδομένων**: Το `WordPositionsProvider` καλεί `/words-positions`, αποθηκεύοντας ανά σελίδα τις λέξεις, τα κουτιά και προαιρετικά `width/height`.
2. **Κλιμάκωση σε οθόνη**: Το `TextBox` παίρνει τα κουτιά της τρέχουσας σελίδας και τα περνά από `calculateScaledPositions`, που συνδυάζει διαστάσεις server και πραγματικό DOM καμβά. Το αποτέλεσμα είναι ο πίνακας `wordsScreenPositions`.
3. **Trigger ανίχνευσης**:
   - **Eye-tracker**: Όταν το hover-debug είναι ανενεργό, οι μετρήσεις βλέμματος γίνονται hit-test πάνω στα `wordsScreenPositions`. Αν βρεθεί λέξη, ορίζεται `currentWord` και ενεργοποιείται μετάφραση.
   - **Hover debug**: Όταν η νέα ρύθμιση είναι ενεργή, ο ακροατής `mousemove` κάνει το ίδιο hit-test με τον κέρσορα. Επιτυχία ορίζει το ίδιο `currentWord` και ενεργοποιεί μετάφραση, αποτυχία την απενεργοποιεί.
4. **Ανάκτηση & εμφάνιση μετάφρασης**: Μόλις `shouldTranslate` γίνει true (από gaze ή hover), ζητείται μετάφραση για το `currentWord.word` και εμφανίζεται το popup στη θέση του κουτιού.

## Τι αποδεικνύει το debug overlay
- Το `WordBoxesOverlay` ζωγραφίζει τις ίδιες κλιμακωμένες συντεταγμένες που χρησιμοποιούν τόσο το eye-tracker όσο και το hover debug. Άρα, αν οι κόκκινες οριοθετήσεις ευθυγραμμίζονται με το κείμενο, η ανίχνευση/μετάφραση λειτουργεί με τις ίδιες ακριβώς συντεταγμένες.

